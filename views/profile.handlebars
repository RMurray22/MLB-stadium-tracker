<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{username}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Pick your Favorite team:</h3>

    <form class="form update-profile-form">
      <div class="form-group">
        <label for="favorite-team">Favorite Team:</label>
        <select name="favorite-team" class="form-input" id="favorite-team">
          <option value="" selected="selected">Select Favorite Team</option>
        </select>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Save</button>
      </div>
    </form>
  </div>

  {{!-- {{#if stadiums.length}}
  <div class="col-md-6 project-list">
    <h3>Stadiums visted:</h3>

    {{#each stadiums as |stadium|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/project/{{stadium.id}}">{{stadium.name}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{stadium.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}} --}}
</div>

<script src="./js/profile.js"></script>